# Galka LIVE

Fail-closed Hyperliquid LIVE engine. Main operating and rollout instructions are in:

```text
HARDENED_LIVE_README_RU.md
LIVE_HYPERLIQUID.md
```

LIVE is disabled by default. The secret config remains outside the repository at `~/.config/galka-live.env` with mode `0600`.
