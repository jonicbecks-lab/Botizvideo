(() => {
'use strict';
const charts=window.LightweightCharts;if(!charts?.createChart)return;const originalCreateChart=charts.createChart.bind(charts),SVG_NS='http://www.w3.org/2000/svg',MAX_STRUCTURE_BARS=240,CONTEXT_BARS=20;
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));const finite=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f;
function createSvg(tag,className){const node=document.createElementNS(SVG_NS,tag);if(className)node.setAttribute('class',className);return node}
function formatPrice(v){const n=finite(v);if(!(n>0))return'—';if(n>=1000)return n.toFixed(2);if(n>=1)return n.toFixed(4).replace(/0+$/,'').replace(/\.$/,'');return Number(n.toPrecision(7)).toString()}
function rowPayload(row){if(!row||!(Number(row.time)>0))return null;const out={timeMs:Math.round(Number(row.time)*1000),open:finite(row.open),high:finite(row.high),low:finite(row.low),close:finite(row.close),volume:Math.max(0,finite(row.volume))};return out.open>0&&out.high>0&&out.low>0&&out.close>0?out:null}
function nearestIndex(all,timeSec){if(!all.length)return-1;let low=0,high=all.length-1,target=Number(timeSec);while(low<=high){const middle=(low+high)>>1,value=Number(all[middle]?.time||0);if(value===target)return middle;if(value<target)low=middle+1;else high=middle-1}const left=clamp(high,0,all.length-1),right=clamp(low,0,all.length-1);return Math.abs(Number(all[left]?.time||0)-target)<=Math.abs(Number(all[right]?.time||0)-target)?left:right}
function estimateInterval(all){for(let i=all.length-1;i>0;i--){const d=Number(all[i]?.time||0)-Number(all[i-1]?.time||0);if(d>0)return d}return 60}
function installStructureDraft(chart,container){if(!chart?.canvas||chart.__galkaMemStructureDraftInstalled)return chart;chart.__galkaMemStructureDraftInstalled=true;const canvas=chart.canvas;
const overlay=createSvg('svg','galka-structure-overlay hidden');overlay.setAttribute('preserveAspectRatio','none');const fullLine=createSvg('line','galka-structure-line'),shape=createSvg('polyline','galka-structure-shape'),leftGuide=createSvg('line','galka-structure-guide'),rightGuide=createSvg('line','galka-structure-guide');
const anchorHandle=createSvg('g'),anchorRing=createSvg('circle','galka-structure-anchor-ring'),anchorMark=createSvg('path','galka-structure-anchor-mark');anchorRing.setAttribute('r','10');anchorMark.setAttribute('d','M0 -6 A2 2 0 1 1 0 -2 M0 -2 L0 6 M-4 0 L4 0 M-7 3 C-6 7 -3 9 0 9 C3 9 6 7 7 3 M-7 3 L-4 5 M7 3 L4 5');anchorHandle.append(anchorRing,anchorMark);
const leftHandle=createSvg('path','galka-structure-boundary galka-structure-boundary-left'),rightHandle=createSvg('path','galka-structure-boundary galka-structure-boundary-right');leftHandle.setAttribute('d','M0 -8 L8 0 L0 8 L-8 0 Z');rightHandle.setAttribute('d','M0 -8 L8 0 L0 8 L-8 0 Z');overlay.append(fullLine,shape,leftGuide,rightGuide,leftHandle,rightHandle,anchorHandle);container.append(overlay);
const toolbar=document.createElement('div');toolbar.className='galka-structure-toolbar hidden';const info=document.createElement('div');info.className='galka-structure-info';const cancel=document.createElement('button');cancel.type='button';cancel.textContent='Отмена';const confirm=document.createElement('button');confirm.type='button';confirm.className='galka-structure-confirm';confirm.textContent='Подтвердить';confirm.disabled=true;toolbar.append(info,cancel,confirm);container.append(toolbar);
const state={active:false,coin:'',interval:'',galkaPrice:0,anchorTime:0,leftTime:0,leftPrice:0,rightTime:0,rightPrice:0,phase:'idle',pointerId:null,startedAtMs:0,researchSetup:null};
const currentInput=()=>document.getElementById('galkaInput');const action=()=>document.getElementById('previewButton');const selectedCoin=()=>String(document.getElementById('symbolSelect')?.value||'').toUpperCase();const selectedInterval=()=>String(document.getElementById('intervalSelect')?.value||'1m');const allRows=()=>Array.isArray(chart.series?.data)?chart.series.data:[];
function timeAtX(x){const w=chart.visibleWindow?.(),g=chart.geometry?.();if(!w?.all?.length||!g)return 0;const ratio=clamp((Number(x)-g.left)/g.plotWidth,0,1),logical=w.start+ratio*w.count,interval=estimateInterval(w.all),base=Math.floor(logical),fraction=logical-base;let t;if(base<0)t=Number(w.all[0]?.time||0)+base*interval;else if(base>=w.all.length)t=Number(w.all.at(-1)?.time||0)+(base-w.all.length+1)*interval;else t=Number(w.all[base]?.time||0);return t+fraction*interval}
function xForTime(timeSec){const w=chart.visibleWindow?.(),g=chart.geometry?.();if(!w?.all?.length||!g||!(Number(timeSec)>0))return null;const all=w.all,interval=estimateInterval(all),target=Number(timeSec);let logical;if(target<=Number(all[0]?.time||0))logical=(target-Number(all[0]?.time||0))/interval;else if(target>=Number(all.at(-1)?.time||0))logical=all.length-1+(target-Number(all.at(-1)?.time||0))/interval;else{const index=nearestIndex(all,target),base=Number(all[index]?.time||0);logical=index+(target-base)/interval}const x=g.left+(logical-w.start)/w.count*g.plotWidth;return x>=g.left-20&&x<=g.right+20?x:null}
function priceAtY(y){const g=chart.geometry?.(),rows=chart.lastRows?.length?chart.lastRows:chart.visibleWindow?.().rows,range=chart.currentPriceRange?.(rows||[]);if(!g||!range||!(range.span>0))return 0;const c=clamp(Number(y),g.top,g.bottom);return range.max-((c-g.top)/g.plotHeight)*range.span}
function yForPrice(price){const g=chart.geometry?.(),rows=chart.lastRows?.length?chart.lastRows:chart.visibleWindow?.().rows,range=chart.currentPriceRange?.(rows||[]);if(!g||!range||!(range.span>0))return null;return g.top+(range.max-Number(price))/range.span*g.plotHeight}
function setLine(node,x1,y1,x2,y2,visible=true){node.setAttribute('visibility',visible?'visible':'hidden');if(!visible)return;node.setAttribute('x1',String(x1));node.setAttribute('y1',String(y1));node.setAttribute('x2',String(x2));node.setAttribute('y2',String(y2))}
function setTransform(node,x,y,visible=true){node.setAttribute('visibility',visible?'visible':'hidden');if(visible)node.setAttribute('transform',`translate(${x} ${y})`)}function setShape(points,visible=true){shape.setAttribute('visibility',visible?'visible':'hidden');if(visible)shape.setAttribute('points',points.map(([x,y])=>`${x},${y}`).join(' '))}
function structureBarCount(){const all=allRows();if(!all.length||!state.leftTime||!state.rightTime)return 0;let left=nearestIndex(all,state.leftTime),right=nearestIndex(all,state.rightTime);if(left<0||right<0)return 0;if(left>right)[left,right]=[right,left];return right-left+1}
function updateToolbar(){if(!state.active||['idle','ready','locked'].includes(state.phase)){toolbar.classList.add('hidden');return}toolbar.classList.remove('hidden');if(state.phase==='choose-left'){info.innerHTML=`<b>⚓ ${formatPrice(state.galkaPrice)}</b> · двигай левую границу`;confirm.textContent='Левая ✓';confirm.disabled=!(state.leftTime>0&&state.leftPrice>0);return}if(state.phase==='choose-right'){const count=structureBarCount();info.innerHTML=`<b>⚓ ${formatPrice(state.galkaPrice)}</b> · двигай правую границу · ${count} свеч.`;confirm.textContent='Готово GALKA';confirm.disabled=!(state.rightTime>0&&state.rightPrice>0)||count<1}}
function render(){if(!state.active||state.coin!==selectedCoin()){overlay.classList.add('hidden');toolbar.classList.add('hidden');return}const g=chart.geometry?.(),anchorY=yForPrice(state.galkaPrice);if(!g||anchorY==null){overlay.classList.add('hidden');updateToolbar();return}overlay.classList.remove('hidden');overlay.setAttribute('viewBox',`0 0 ${g.width} ${g.height}`);const locked=['ready','locked'].includes(state.phase);setLine(fullLine,g.left,anchorY,g.right,anchorY,!locked);const anchorX=xForTime(state.anchorTime),leftX=xForTime(state.leftTime),rightX=xForTime(state.rightTime),leftY=yForPrice(state.leftPrice),rightY=yForPrice(state.rightPrice);setTransform(anchorHandle,anchorX??0,anchorY,anchorX!=null);setTransform(leftHandle,leftX??0,leftY??0,leftX!=null&&leftY!=null);setTransform(rightHandle,rightX??0,rightY??0,rightX!=null&&rightY!=null&&state.phase!=='choose-left');setLine(leftGuide,leftX??0,g.top,leftX??0,g.bottom,leftX!=null&&!locked);setLine(rightGuide,rightX??0,g.top,rightX??0,g.bottom,rightX!=null&&state.phase!=='choose-left'&&!locked);const points=[];if(leftX!=null&&leftY!=null)points.push([leftX,leftY]);if(anchorX!=null)points.push([anchorX,anchorY]);if(rightX!=null&&rightY!=null&&state.phase!=='choose-left')points.push([rightX,rightY]);setShape(points,points.length>=2);updateToolbar()}
function resetState({keepInput=false}={}){state.active=false;state.coin='';state.interval='';state.galkaPrice=0;state.anchorTime=0;state.leftTime=0;state.leftPrice=0;state.rightTime=0;state.rightPrice=0;state.phase='idle';state.pointerId=null;state.startedAtMs=0;state.researchSetup=null;chart.crosshair=null;overlay.classList.add('hidden');toolbar.classList.add('hidden');if(!keepInput){const input=currentInput();if(input&&!input.disabled)input.value=''}chart.draw?.();document.dispatchEvent(new CustomEvent('galka:mem-structure-reset'))}
function beginFromCrosshair(event){if(state.active&&state.phase!=='idle')return false;const input=currentInput();if(!input||input.disabled)return false;const galkaPrice=finite(event.detail?.price);if(!(galkaPrice>0))return false;const crosshairX=finite(chart.crosshair?.x,NaN),anchorTime=Number(event.detail?.timeSec||(Number.isFinite(crosshairX)?timeAtX(crosshairX):0));if(!(anchorTime>0))return false;state.active=true;state.coin=selectedCoin();state.interval=selectedInterval();state.galkaPrice=galkaPrice;state.anchorTime=anchorTime;state.leftTime=anchorTime;state.leftPrice=galkaPrice;state.rightTime=0;state.rightPrice=0;state.phase='choose-left';state.pointerId=null;state.startedAtMs=Date.now();state.researchSetup=null;input.value=formatPrice(galkaPrice);const b=action();if(b){b.disabled=true;b.textContent='Разметка GALKA'}render();if(navigator.vibrate)navigator.vibrate(12);return true}
function stopCanvasEvent(event){event.preventDefault();event.stopImmediatePropagation()}
function updateActiveBoundary(point){if(!point||point.zone!=='plot')return;const time=timeAtX(point.x),p=priceAtY(point.y);if(!(time>0&&p>0))return;if(state.phase==='choose-left'){state.leftTime=Math.min(time,state.anchorTime);state.leftPrice=p}else if(state.phase==='choose-right'){state.rightTime=Math.max(time,state.anchorTime);state.rightPrice=p}chart.setCrosshair?.({...point,zone:'plot'});chart.draw?.()}
canvas.addEventListener('galka:select-price',event=>{if(!beginFromCrosshair(event))return;event.preventDefault();event.stopImmediatePropagation()},{capture:true});
canvas.addEventListener('pointerdown',event=>{if(!state.active||!['choose-left','choose-right'].includes(state.phase))return;const point=chart.pointFromEvent?.(event);if(!point||point.zone!=='plot')return;stopCanvasEvent(event);state.pointerId=event.pointerId;updateActiveBoundary(point);try{canvas.setPointerCapture(event.pointerId)}catch(_){}render()},{capture:true});
canvas.addEventListener('pointermove',event=>{if(!state.active||!['choose-left','choose-right'].includes(state.phase)||state.pointerId!==event.pointerId)return;const point=chart.pointFromEvent?.(event);if(!point||point.zone!=='plot')return;stopCanvasEvent(event);updateActiveBoundary(point);render()},{capture:true});
function finishPointer(event){if(!state.active||state.pointerId!==event.pointerId)return;stopCanvasEvent(event);state.pointerId=null;try{if(canvas.hasPointerCapture?.(event.pointerId))canvas.releasePointerCapture(event.pointerId)}catch(_){}render();if(navigator.vibrate)navigator.vibrate(8)}canvas.addEventListener('pointerup',finishPointer,{capture:true});canvas.addEventListener('pointercancel',finishPointer,{capture:true});
function contextRows(){const all=allRows();if(!all.length||!state.leftTime||!state.rightTime)return null;let left=nearestIndex(all,state.leftTime),right=nearestIndex(all,state.rightTime);if(left<0||right<0)return null;if(left>right)[left,right]=[right,left];const anchor=nearestIndex(all,state.anchorTime),totalBars=right-left+1,structureStart=totalBars>MAX_STRUCTURE_BARS?right-MAX_STRUCTURE_BARS+1:left;return{all,left,right,anchor,totalBars,structureBars:all.slice(structureStart,right+1).map(rowPayload).filter(Boolean),structureBarsTruncated:totalBars>MAX_STRUCTURE_BARS,preContextBars:all.slice(Math.max(0,left-CONTEXT_BARS),left).map(rowPayload).filter(Boolean),postContextBars:all.slice(right+1,Math.min(all.length,right+1+CONTEXT_BARS)).map(rowPayload).filter(Boolean)}}
function buildResearchSetup(){const c=contextRows();if(!c)throw new Error('Не удалось прочитать свечи выбранной GALKA');return{schemaVersion:2,selectionMethod:'manual_crosshair_structure_v2',symbol:state.coin,timeframe:state.interval,galkaLevel:state.galkaPrice,anchorTimeMs:Math.round(state.anchorTime*1000),anchorPrice:state.galkaPrice,leftBoundaryTimeMs:Math.round(state.leftTime*1000),leftBoundaryPrice:state.leftPrice,rightBoundaryTimeMs:Math.round(state.rightTime*1000),rightBoundaryPrice:state.rightPrice,structureStartTimeMs:Math.round(state.leftTime*1000),structureEndTimeMs:Math.round(state.rightTime*1000),selectedAtMs:Date.now(),draftStartedAtMs:state.startedAtMs,fullStructureBarCount:c.totalBars,structureBarsTruncated:c.structureBarsTruncated,anchorCandle:c.anchor>=0?rowPayload(c.all[c.anchor]):null,leftBoundaryCandle:rowPayload(c.all[c.left]),rightBoundaryCandle:rowPayload(c.all[c.right]),structureEndCandle:rowPayload(c.all[c.right]),preContextBars:c.preContextBars,structureBars:c.structureBars,postContextBarsAtPlacement:c.postContextBars,lockedForCampaign:false}}
function finishStructure(){if(!state.active||state.phase!=='choose-right'||!state.rightTime)return;try{state.researchSetup=buildResearchSetup();state.phase='ready';chart.crosshair=null;render();const detail={coin:state.coin,interval:state.interval,galkaPrice:state.galkaPrice,anchorTime:state.anchorTime,leftTime:state.leftTime,leftPrice:state.leftPrice,rightTime:state.rightTime,rightPrice:state.rightPrice,researchSetup:state.researchSetup};document.dispatchEvent(new CustomEvent('galka:mem-structure-ready',{detail}));if(navigator.vibrate)navigator.vibrate(12)}catch(error){const toast=document.getElementById('toast');if(toast){toast.textContent=error.message;toast.className='toast error';setTimeout(()=>toast.classList.add('hidden'),4500)}}}
function confirmStage(){if(!state.active)return;if(state.phase==='choose-left'){if(!(state.leftTime>0&&state.leftPrice>0))return;state.phase='choose-right';state.rightTime=state.anchorTime;state.rightPrice=state.galkaPrice;render();if(navigator.vibrate)navigator.vibrate(10);return}if(state.phase==='choose-right')finishStructure()}
cancel.addEventListener('click',()=>resetState());confirm.addEventListener('click',confirmStage);document.getElementById('symbolSelect')?.addEventListener('change',()=>{if(state.active)resetState()});document.getElementById('intervalSelect')?.addEventListener('change',()=>{if(state.active)resetState()});
const originalDraw=chart.draw.bind(chart);chart.draw=(...args)=>{const result=originalDraw(...args);requestAnimationFrame(render);return result};
window.GalkaMemStructureDraft={active:()=>state.active&&['choose-left','choose-right'].includes(state.phase),ready:()=>state.phase==='ready',cancel:resetState,snapshot:()=>state.active?{coin:state.coin,timeframe:state.interval,galkaLevel:state.galkaPrice,anchorTimeMs:state.anchorTime*1000,leftBoundaryTimeMs:state.leftTime*1000,leftBoundaryPrice:state.leftPrice,rightBoundaryTimeMs:state.rightTime*1000,rightBoundaryPrice:state.rightPrice,phase:state.phase,researchSetup:state.researchSetup}:null,lock(){if(state.active){state.phase='locked';render()}},unlock(){if(state.phase==='locked'){state.phase='ready';render()}}};return chart}
window.LightweightCharts=Object.freeze({...charts,createChart(container,options){return installStructureDraft(originalCreateChart(container,options),container)}});
})();
